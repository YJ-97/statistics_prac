# 아래의 확률분포에서 추출된 확률분포의 표본평균분포
# N = 10, 30, 50, 100, 반복 = 10000

# 1. 지수분포(rexp(N)) : 평균 = 1, 분산 = 1

# 2. cauchy분포(rcauchy(N)) : 평균, 분산 존재 x

# 3. P(X=0)=2/5, P(X=1)=2/5, P(X=2)=1/5

# histogram을 각각 그리고 근사정규분포 곡선을 표시 (2번은 곡선 x)

# 0.025, 0.05, 0.95, 0.975 에 해당하는 분위수 비교